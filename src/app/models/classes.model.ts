export interface classes {
    id: string,
    title: string,
    instructor: string,
    description: string,
    duration: string,
    students: number,
    ratings: number,
    level: string,
    image: string
}